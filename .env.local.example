# 複製成 .env.local 後填入自己的值（.env.local 已被 .gitignore 忽略）。
# 這份範例檔不含任何真實憑證，可以安全 commit。
#
# BILI_COOKIE：截圖腳本（npm run capture-screenshots）用的 bilibili 登入 cookie。
#   有填 → 帶登入狀態開影片頁，拿得到高畫質（截圖裡的 qn 會是 1080P 以上）
#   沒填或整個檔案不存在 → 維持未登入，畫質約 480P，其餘流程不變
#
# 取得方式：登入 bilibili → DevTools → Network → 任一請求 → Request Headers → 複製整段 Cookie。
# 至少要有 SESSDATA；建議連 bili_jct、DedeUserID、CURRENT_QUALITY 一起帶。
# SESSDATA 約數個月過期，截圖畫質掉回 480P 就是該重新複製了。
#
# ⚠️ SESSDATA 等同帳號憑證，外洩等於帳號被登入。不要 commit、不要貼到聊天室或 issue。
BILI_COOKIE="SESSDATA=...; bili_jct=...; DedeUserID=...; CURRENT_QUALITY=120"
